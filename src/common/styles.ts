import { StyleSheet } from 'react-native';

const CommonStyles = StyleSheet.create({
  container: {
    flex: 1,
  },
  text14: {
    fontSize: 14,
    color: '#000',
  },
});

export default CommonStyles;
